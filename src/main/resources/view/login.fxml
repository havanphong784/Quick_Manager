<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>
<HBox prefHeight="600" prefWidth="900" styleClass="qm-page"
      xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.quickmanager.controller.LoginController">

    <Region HBox.hgrow="ALWAYS" styleClass="auth-background"/>

    <VBox fx:id="loginPanel" alignment="CENTER_LEFT" prefWidth="440.0" spacing="16.0" styleClass="qm-content"
          style="-fx-padding: 0 60 0 60;">
        <HBox alignment="CENTER_LEFT" spacing="12.0">
            <ImageView fitHeight="32.0" fitWidth="32.0" preserveRatio="true">
                <Image url="@../images/logo.png"/>
            </ImageView>
            <Label text="Quick Manager" styleClass="logo"/>
        </HBox>

        <Label text="Rất vui được gặp lại" styleClass="qm-page-title"
               style="-fx-font-size: 28; -fx-padding: 8 0 16 0;"/>

        <VBox spacing="8.0">
            <Label text="Tên đăng nhập" styleClass="qm-label-muted" style="-fx-font-weight: bold; -fx-font-size: 12;"/>
            <TextField fx:id="usernameField" promptText="Nhập tên đăng nhập" styleClass="qm-input" prefHeight="40.0"/>
        </VBox>

        <VBox spacing="8.0">
            <Label text="Mật khẩu" styleClass="qm-label-muted" style="-fx-font-weight: bold; -fx-font-size: 12;"/>
            <PasswordField fx:id="passwordField" promptText="Nhập mật khẩu" styleClass="qm-input" prefHeight="40.0"/>
        </VBox>


        <Button fx:id="loginButton" onAction="#handleLogin" defaultButton="true" prefHeight="42.0" maxWidth="Infinity"
                text="Đăng nhập" styleClass="btn-blue"/>


        <HBox alignment="CENTER" spacing="4.0">
            <Label text="Chưa có tài khoản?" styleClass="qm-label-muted"/>
            <Hyperlink onAction="#handleRegister" text="Đăng ký ngay" styleClass="qm-hint"
                       style="-fx-text-fill: -qm-accent-blue;"/>
        </HBox>

        <Label fx:id="statusLabel" text="" styleClass="qm-warning-value"/>
    </VBox>
</HBox>
